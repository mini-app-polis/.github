# SEC-001. The same gitleaks the shared workflow runs in CI, so a secret
# is caught before it is committed rather than after it is pushed.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.28.0
    hooks:
      - id: gitleaks
